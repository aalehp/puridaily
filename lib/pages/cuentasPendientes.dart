import 'package:flutter/material.dart';

class CuentasPendientes extends StatelessWidget {
  const CuentasPendientes({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}